---
- name : Docker maintenance job
  cron:
    user: root
    name: 'Remove docker unused data'
    special_time: '{{ docker_arg.interval }}'
    job: '/bin/docker system prune -af --filter "until={{ docker_arg.keep }}" &> /dev/null'
